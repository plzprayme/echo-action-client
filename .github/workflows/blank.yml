name: use echo action

on:
  push:
    branches: [ master ]

jobs:
  echo:
    runs-on: ubuntu-latest

    steps:
      - uses: plzprayme/echo-action-server@master
        with:
          say: 클둥이 안녕~
